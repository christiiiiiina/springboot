spring:
  profiles:
    active: prod
  mvc:
    view:
      prefix: /
      suffix: .jsp
#    启用支持put，delete请求
    hiddenmethod:
      filter:
        enabled: true

server:
  servlet:
    encoding:
      charset: utf-8
      force: true